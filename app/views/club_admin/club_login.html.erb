<% provide(:title, 'Club Sign In') %>
<div id="content">
  <div class="inner-content" id="myContent">
    <div class="single-box">
      <div class="project-post-content">
        <h1>Sign In to Manage Your Club</h1>
        <%= render 'shared/ClubAdmin/club_login_error' %>
        <div class="text-fields newfont">
          <%= form_tag (club_admin_login_path) do %>
              <span><i class="fa fa-user"></i></span><%= label_tag :club_login_id, "Club Login ID" %>
              <%= text_field_tag :club_login_id, nil, class: "form-control", required: '' %>

              <span><i class="fa fa-lock"></i></span><%= label_tag :password, "Password" %>
              <%= password_field_tag :password, nil, class: "form-control", required: '' %>
              <br>

              <p align="center"><%= submit_tag "Sign In", class: "btn btn-success btn-lg" %></p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>